PORT=8787
HOST=0.0.0.0
DATA_DIR=./data
# Optional. When omitted, the service creates /data/session.key automatically.
# To supply your own key, use 32 bytes encoded as 64 hexadecimal characters.
ECHOCRATE_SECRET=
PUBLIC_ORIGIN=http://localhost:8787
